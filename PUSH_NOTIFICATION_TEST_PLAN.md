# 푸시 알림 실기기 테스트 절차

이 문서는 `google-services.json` 적용과 Functions 배포가 끝난 뒤,
실제 기기에서 FCM 알림 수신을 검증하기 위한 절차입니다.
임의로 성공 여부를 판단하지 말고, 아래 각 항목을 실제로 수행한 결과만 기록하세요.

## 사전 준비

- 최소 2개 지점(예: 부산/대구)에 각각 1명 이상의 테스트 계정 필요
- 각 지점에 관리자 1명, 직원 2명 이상 로그인 가능한 상태
- 테스트 기기 최소 1대 (가능하면 지점당 1대씩 2대 이상 권장)
- `adb install -r app-debug.apk`로 최신 APK 설치 완료
- 최초 로그인 시 Android 알림 권한 팝업에서 **허용** 선택

## A. 앱 상태별 수신 확인

같은 긴급 알림 1건을 아래 4가지 상태에서 각각 재전송하며 확인합니다.

| # | 앱 상태 | 수행 방법 | 기대 결과 | 실제 결과 |
|---|---|---|---|---|
| A1 | 앱 실행 중(포그라운드) | 앱을 열어둔 채로 관리자가 긴급 알림 전송 | 앱 내부 배너(`foreground-notification`)로 표시, 중복 시스템 알림 없음 | |
| A2 | 앱 백그라운드 | 홈 버튼으로 앱을 백그라운드로 보낸 뒤 전송 | Android 시스템 알림으로 표시, 탭하면 앱 복귀 | |
| A3 | 화면 꺼짐/잠금 | 화면을 끄거나 잠근 상태에서 전송 | 잠금 화면에 알림 표시, 소리/진동 발생 | |
| A4 | 최근 앱에서 완전히 종료 | 최근 앱 목록에서 위로 밀어 앱 종료 후 전송 | 시스템 알림 수신 (기기 제조사 배터리 최적화로 지연/누락 가능성은 알려진 제약) | |

각 항목에서 알림을 탭했을 때:
- 직원 계정으로 로그인된 상태 → 번호판 메인 화면으로 이동
- 관리자 계정으로 로그인된 상태 → 관리자 화면으로 이동
- 로그인이 풀려 있었다면 로그인 후 원래 목적 화면으로 이동하는지 확인

## B. 긴급 알림 대상 판정 확인

동일 지점(예: 부산) 안에서 아래 3가지 대상 유형을 각각 테스트합니다.
매 테스트 전 번호판 상태를 원하는 조건에 맞게 세팅하세요.

| # | 시나리오 | 준비 상태 | 전송 대상 | 확인할 것 |
|---|---|---|---|---|
| B1 | 번호 점유 직원만 | 직원 A는 번호 보유, 직원 B는 미보유 | "번호 보유 직원" 선택 후 전송 | A만 수신, B는 수신 안 함 |
| B2 | 번호 미점유 직원만 | 직원 A는 번호 보유, 직원 B는 미보유 | "번호 미점유 직원" 선택 후 전송 | B만 수신, A는 수신 안 함 |
| B3 | 지점 전체 직원 | 점유 여부 무관 | "지점 전체 직원" 선택 후 전송 | A, B 모두 수신 |
| B4 | 점유 직원이 0명일 때 | 아무도 번호를 보유하지 않은 상태 | "번호 보유 직원" 선택 | 전송 버튼 클릭 시 "현재 번호를 보유 중인 직원이 없습니다" 안내만 표시되고 실제 전송은 되지 않음 |
| B5 | 같은 직원의 여러 기기 | 직원 A가 휴대폰 2대에서 각각 로그인 | 아무 대상이나 선택 후 전송 | 두 기기 모두 수신 (중복 전송 없이 각 1회씩) |
| B6 | 토큰이 없는 직원 | 앱을 설치하지 않은/한 번도 로그인 안 한 명단 직원 | 지점 전체 전송 | 결과 메시지의 "FCM 토큰이 없는 직원" 수에 반영되는지 확인 |

## C. 지점 간 격리 확인

| # | 시나리오 | 확인할 것 |
|---|---|---|
| C1 | 부산 관리자가 지점 전체 알림 전송 | 부산 직원만 수신, 대구/청주/김포 직원은 수신 안 함 |
| C2 | 대구 관리자가 긴급 알림 전송 | 대구 직원만 수신, 다른 지점 미수신 |
| C3 | 서로 다른 지점 직원이 같은 시간대에 로그인 중 | 한쪽 지점 알림이 다른 지점 기기에 전혀 나타나지 않는지 확인 |

## D. 일반(지점 전체) 알림 확인

| # | 시나리오 | 확인할 것 |
|---|---|---|
| D1 | 제목/내용을 채우고 전송 | `branch_notices` 채널로 수신, 일반 소리/진동 (긴급과 다른 톤) |
| D2 | 제목 또는 내용을 비운 채 전송 시도 | 전송 버튼이 막히고 "제목과 내용을 모두 입력해주세요" 안내 |

## E. 권한/설정 예외 확인 (참고용, 실패해도 정상)

- Android 설정에서 알림 권한을 직접 차단한 뒤 로그인 → 앱 상단에 권한 꺼짐 안내 배너가 뜨는지만 확인 (수신 자체는 당연히 안 됨)
- 기기 배터리 최적화에서 앱을 "제한됨"으로 설정한 경우 백그라운드/종료 상태 수신이 지연되거나 누락될 수 있음 — 이는 Android 정책상 알려진 제약이며 버그가 아님

## 기록 방법

각 표의 "실제 결과" 칸에 성공/실패와 시각을 적어 남겨두면,
2단계(Authentication 마이그레이션) 진행 여부를 판단하는 근거로 사용합니다.
